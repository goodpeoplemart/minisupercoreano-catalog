# Minisuper Coreano Catalog Development Rules

## 핵심 원칙
- 기존 기능을 유지한다.
- 전체 파일을 재작성하지 않는다.
- 필요한 최소 범위만 수정한다.
- 수정 전에 변경할 파일 목록과 이유를 먼저 보고한다.
- 사용자의 승인 없이 수정 범위를 확대하지 않는다.
- 관련 없는 파일은 수정하지 않는다.

## 고정 선택자
- 기존 CSS 클래스 이름을 임의로 변경하지 않는다.
- 기존 data-msc-* 속성을 변경하지 않는다.
- 기존 id를 임의로 변경하지 않는다.
- 새 선택자를 만들기 전에 기존 선택자를 전체 검색한다.
- 동일 기능에 중복 CSS 클래스를 만들지 않는다.
- 선택자를 변경해야 할 경우 Liquid, CSS, JavaScript의 모든 사용처를 함께 확인한다.

## Liquid
- catalog.liquid는 정적 셸, API 설정, data-msc-* 훅을 담당한다.
- JavaScript가 사용하는 data-msc-* 훅을 삭제하거나 변경하지 않는다.
- Liquid 구조를 변경할 때 CSS와 JavaScript 영향을 먼저 분석한다.

## CSS
- 기존 58개 CSS 클래스는 고정된 공개 인터페이스로 취급한다.
- Liquid에 직접 없는 동적 클래스도 삭제하지 않는다.
- JavaScript의 classList.add, classList.remove, classList.toggle 사용 여부를 확인한다.
- 새 스타일은 기존 클래스 내부에 추가하는 것을 우선한다.
- 불필요한 !important 사용을 피한다.
- 반응형 규칙을 수정할 때 데스크톱과 모바일 영향을 모두 확인한다.

## JavaScript
- data-msc-* 중심 DOM 연결 구조를 유지한다.
- App Proxy API 경로와 응답 구조를 임의로 변경하지 않는다.
- API 호출 3개의 경로, 요청 방식, 응답 필드를 변경하지 않는다.
- 기존 함수 전체를 교체하지 말고 필요한 함수 또는 코드 블록만 수정한다.
- DOM 요소를 추가할 때 Liquid와 CSS 연결을 함께 확인한다.
- 동적 생성 클래스는 CSS 존재 여부를 확인한다.

## 작업 절차
1. 먼저 분석한다.
2. 수정할 파일과 코드 구역을 보고한다.
3. 최소 변경 계획을 제시한다.
4. 승인된 범위만 수정한다.
5. 변경 파일과 변경 함수 또는 CSS 영역을 보고한다.
6. 오류와 경고를 숨기지 않는다.
7. 기존 기능 테스트 항목을 보고한다.

## 금지 사항
- 전체 파일 덮어쓰기 금지
- 기존 클래스 일괄 이름 변경 금지
- data-msc-* 일괄 변경 금지
- App Proxy 설정 임의 변경 금지
- shopify.app.toml 임의 변경 금지
- package.json 의존성 임의 변경 금지
- .env 또는 비밀정보 열람·출력 금지
- 사용자의 승인 없는 파일 삭제 금지
- git reset --hard, git clean -fd 같은 파괴적 Git 명령 금지
